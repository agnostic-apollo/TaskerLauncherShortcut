<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="50dp">
    <ImageView android:id="@+id/list_item_shortcut_icon"
        android:layout_width="60dp"
        android:layout_height="wrap_content"
        android:padding="4dp"
        android:scaleType="fitCenter"
        android:contentDescription="@string/shortcut_icon" />

    <LinearLayout
        android:id="@+id/layout_list_item"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:orientation="vertical" >

        <TextView
            android:id="@+id/list_item_app_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="16sp"
            android:gravity="start|center_vertical"
            android:padding="2dp"
            tools:text="@string/app_label"/>

        <TextView
            android:id="@+id/list_item_shortcut_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textStyle="normal"
            android:textSize="12sp"
            android:gravity="start|center_vertical"
            android:padding="2dp"
            tools:text="@string/shortcut_label"/>
    </LinearLayout>
</LinearLayout>